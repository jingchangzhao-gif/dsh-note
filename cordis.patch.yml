# dsh-note bundle registration: mounts one instance of the note plugin when
# this package joins a dsh profile's bundle stack (dsh plugin add dsh-note).
- insert:
    - id: dsh-note
      name: "dsh-note"
